angular.module('HomeCtrl', []).controller('HomeController', function($scope) {
    $scope.title = "Welcome to Phonebook!";
    $scope.description = "Why saving contacts to database to which someone else own? Save contacts to your own database.";
});